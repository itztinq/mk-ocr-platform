# Backend API base URL.
# For local development outside Docker, set this to the backend address.
# When empty (or not set), API calls use same-origin (relative URLs)
# which works when Nginx proxies requests to the backend (Docker setup).
VITE_API_BASE_URL=http://127.0.0.1:8000
